name: HF Space Keepalive

on:
  schedule:
    - cron: '0 12 */4 * *'  # Every 4 days at noon UTC
  workflow_dispatch:         # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Bella demo
        run: |
          curl -f "https://jrbissell1979-bella-demo.hf.space/api/health"
          echo "Bella demo ping OK"
